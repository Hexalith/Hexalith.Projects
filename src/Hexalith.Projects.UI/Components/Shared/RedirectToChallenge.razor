@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
        => Navigation.NavigateTo(
            $"authentication/challenge?returnUrl={Uri.EscapeDataString(new Uri(Navigation.Uri).PathAndQuery)}",
            forceLoad: true);
}
