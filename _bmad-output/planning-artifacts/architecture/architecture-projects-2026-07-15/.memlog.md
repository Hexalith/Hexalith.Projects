---
scope: Hexalith.Projects whole-module target architecture rebaseline
purpose: Terse build substrate for corrective planning and implementation convergence
altitude: initiative
updated: 2026-07-16T04:56
---

- (direction) Coaching path selected; architecture spine is the only deliverable.
- (constraint) The finalized PRD package at _bmad-output/planning-artifacts/prds/prd-Hexalith.Projects-2026-05-24/ is the product authority for the architecture update.
- (constraint) The approved 2026-07 sprint-change proposals are architecture inputs; the 2026-07-15 planning rebaseline controls where earlier proposals conflict or are superseded.
- (constraint) This run replaces the stale canonical architecture for Hexalith.Projects while preserving completed Epics 1-5 and existing implementation as historical evidence, not release authorization.
- (constraint) Corrective implementation and production release remain blocked until the rebaselined planning set passes implementation readiness; committed event history may not be rewritten and unsafe dual writes are forbidden.
- (event) architecture coaching run resumed on 2026-07-16
- (constraint) The approved 2026-07-16 downstream-artifact-repair proposal amends the saved run and is binding architecture input; it preserves the final PRD and requires the architecture spine before UX, epic, test-design, readiness, or sprint repair.
- (decision) [ADOPTED] Paradigm binds Hexalith.Projects to a domain-centric Hexalith.EventStore DomainService with platform-owned runtime and presentation adapters, preventing Projects from recreating hosting, Dapr persistence/publication, projection plumbing, cursors, health, telemetry, topology, or surface composition.
- (decision) [ADOPTED] Ownership binds Project domain policy, stable contracts, handlers, and Project-specific durable-task transitions to Hexalith.Projects; generic durable-workflow capability to EventStore/platform; descriptors to non-packable Hexalith.Projects.UI.Contracts; runtime Web/MCP/CLI composition to FrontComposer/platform hosts; and end-user candidate/proposal presentation to Hexalith.Chatbot, preventing duplicate or expanded authority.
- (decision) [ADOPTED] No Project is caller-visible or Active until exactly one authorized Folder is bound and the required read model confirms the completed state; Pending creation remains Durable Task state and never becomes a Project lifecycle value, preventing caller-visible folderless Projects and lifecycle/task conflation.
- (decision) [ADOPTED] Durable Tasks use Pending, Running, WaitingForDependency, NeedsAttention, Succeeded, Rejected, Failed, and Cancelled with immutable terminal outcomes; checkpoints, leases, receipts, restart/two-instance convergence, duplicate delivery, compensation, reconciliation, and the irreversible cancellation boundary are durable, and neither HTTP 202 nor SignalR proves completion.
- (decision) [ADOPTED] Consequential operations use server-issued 15-minute tamper-evident atomically single-use Confirmation Artifacts bound to Tenant, actor, action, targets, normalized request hash, Preview, and current versions; idempotency is scoped by Tenant, actor, operation, and key, with same-request replay, changed-request conflict, and at least 30-day or result-lifetime retention.
- (decision) [ADOPTED] Migration preserves committed event history, forbids unsafe dual command writes, compares old and SDK read models deterministically from the same events, cuts over by value slice with routing rollback, and requires separate repository-local approval, version, evidence, entry gate, and rollback for every sibling/platform change.
- (decision) [ADOPTED] Resolution Traces are request-scoped, current-only, and nonpersistent; Safe Diagnostic Export v1 is separately authorized, deterministically bounded to a complete 1 MiB response, 500 references, and 100 newest-first audit rows with no cursor or retention; Web/CLI/MCP/Chatbot share semantics without expanding authority, and consequential autonomous MCP mutation remains gated.
- (version) Local 2026-07-16 EventStore reality check: DomainService exposes the two-line host, IDomainQueryHandler, IDomainProjectionHandler, admission stages, IReadModelStore/read-model batches/freshness, and IQueryCursorCodec; its current Release assemblies expose no generic Durable Task or Confirmation Artifact API, so that capability is an external platform entry gate rather than Projects-local implementation.
- (question) Choose creation choreography that enforces the Folder invariant without introducing a third Project lifecycle or unsafe cross-service atomicity: recommended Folder-first durable orchestration then one Project creation commit containing the authorized Folder binding, versus a hidden incomplete Project stream followed by binding.
- (decision) [ADOPTED] Project creation is Folder-first: a Durable Task creates or validates the authorized Folder, then submits one Project creation commit that already contains the Folder binding; the Project becomes visible only after read-model confirmation. A Folder-created/Project-uncommitted outcome becomes NeedsAttention with resumable reconciliation, and Projects never auto-deletes the Folders-owned resource. This binds creation and prevents Active or caller-visible folderless Projects without inventing a third Project lifecycle.
- (question) Choose durable workflow persistence: recommended platform-owned generic Durable Task records and execution engine with Projects-owned workflow definitions/transitions, versus a Projects event-sourced Task aggregate with only platform scheduling, versus decentralized sibling-event choreography.
- (decision) [ADOPTED] Durable workflow persistence and execution are platform-owned: the EventStore platform supplies generic Durable Task records, task IDs, scoped idempotent admission, leases, checkpoints, step receipts, retries, polling, retention, cancellation enforcement, recovery, and immutable terminal transitions; Hexalith.Projects supplies versioned workflow definitions, domain-specific transition guards, dependency steps, completion predicates, and Recovery Action Codes. This prevents per-domain task runtimes and keeps operational workflow state out of Project event history.
- (question) Choose the single authority for Conversation-to-Project membership: recommended Hexalith.Conversations aggregate as system of record while Projects owns link/move/unlink intent, durable orchestration, audit, and a rebuildable tenant-scoped reverse index; alternative Projects-owned membership would conflict with the existing Conversations ProjectId state and ConversationProjectChanged event or require unsafe dual ownership.
- (decision) [ADOPTED] Hexalith.Conversations is the sole system of record for Conversation-to-Project membership. Hexalith.Projects owns link/move/unlink intent, Project/actor policy, Durable Task orchestration, metadata-only audit, and a rebuildable tenant-scoped reverse index; the Project aggregate stores no Conversation membership. Completion requires authoritative Conversations state plus confirmed Projects read-model convergence, preventing dual-writable membership.
- (decision) [ADOPTED] The Project aggregate owns the Project Folder binding and File/Memory link set as stable references; Folders and Memories exclusively own resource existence, payload, lifecycle, and authorization. Project Context hydrates only current authorized metadata, marks stale/rebuilding/unavailable evidence explicitly, and never copies foreign payloads, preventing ownership duplication and hidden stale-context inclusion.
- (question) Choose cross-context recovery semantics: recommended centrally orchestrated forward-recovery saga using a deterministic task-step idempotency key, expected resource version, durable owner receipt, and re-query-before-retry; compensation is allowed only through an owning service's explicit idempotent compensating command, otherwise partial committed work becomes NeedsAttention. Alternatives are distributed atomicity across services or decentralized event choreography, neither of which supplies safe rollback and task truth for current sibling contracts.
- (decision) [ADOPTED] Cross-context mutations use a centrally orchestrated forward-recovery saga. Every step carries a deterministic task/step idempotency key and expected owner version, persists the owning service's durable receipt before advancing, and queries authoritative status before retrying a lost response. Compensation is permitted only through an explicit idempotent owner command; otherwise a committed partial outcome becomes NeedsAttention. Succeeded requires authoritative end-state plus relevant read-model confirmation, preventing distributed-transaction fiction and destructive foreign rollback.
- (question) Choose Confirmation Artifact representation: recommended opaque high-entropy token referencing a platform-owned durable record whose token is stored only as a protected hash and whose bound fields, expiry, version, consumed task, and consumption state are atomically checked with Durable Task admission; alternative self-contained signed tokens require a second spent-token ledger, while stateless signed tokens cannot enforce single use.
- (decision) [ADOPTED] A Confirmation Artifact is an opaque high-entropy token over a platform-owned durable record; only a protected token hash is stored with Tenant, actor/delegation, action, targets, normalized request hash, Preview digest, current owner versions, schema version, issuance, expiry, consumption state, and admitted task ID. Artifact validation/consumption and Durable Task admission are atomic. Same-key equivalent retries return the admitted task; changed requests conflict; expired, altered, actor/target-swapped, stale-version, or independently replayed artifacts fail closed.
- (question) Choose read-side trust architecture: recommended bounded incremental EventStore read models plus a rebuildable tenant-scoped Reference Trust Index containing only safe owner metadata, owner version/watermark, authorization outcome, and freshness; normal reads include only Current evidence, report stale/rebuilding/unavailable items safely, and explicit refresh uses owner batch APIs with bounded concurrency. Alternatives are per-reference live fan-out, which cannot meet the scale/reliability envelope, or copied foreign payload/authority, which violates ownership.
- (decision) [ADOPTED] Queries use bounded incremental EventStore read models and a rebuildable tenant-scoped Reference Trust Index containing only safe owner metadata, owner version/watermark, authorization outcome, and freshness. Normal Project Context includes only Current evidence and reports stale/rebuilding/unavailable evidence through approved degraded states; explicit refresh uses owner batch APIs with bounded concurrency. Every mutation revalidates with the owning service, so cached evidence never grants write authority.
- (question) Choose Project write aggregate scope for the PRD maximum of 5,000 context references: recommended one Project aggregate stream owning lifecycle, setup, one Folder binding, and the combined File/Memory reference identity set, with Conversation membership external and all query shape in read models. This preserves atomic uniqueness/cap invariants and extends the current design; partitioned reference aggregates would require a second coordinator for the global cap and can diverge. Revisit partitioning only on measured per-Project contention or replay-budget failure.
- (decision) [ADOPTED] One Project aggregate stream owns lifecycle, setup, exactly one Folder binding, and the combined File/Memory reference identity set capped at 5,000 excluding the Folder; Conversation membership remains external. Aggregate commands serialize per Project and enforce uniqueness, cap, lifecycle, and idempotency atomically; all query shape is projected. Partitioning is deferred until measured per-Project contention or replay-budget failure justifies a coordinated migration.
- (question) Choose contract authority: recommended dependency-light versioned .NET contracts in Hexalith.Projects.Contracts as the sole editable domain/wire vocabulary, with platform-generated OpenAPI, clients, JSON schemas, and surface descriptors treated as derived artifacts and checked against a live host; Hexalith.Projects.UI.Contracts references Contracts and owns only non-packable FrontComposer descriptors. Alternative OpenAPI-first ownership would require generated server/domain mappings that the current DomainService SDK does not use; keeping handwritten C# and YAML authorities preserves the verified drift risk.
- (decision) [ADOPTED] Dependency-light versioned .NET contracts in Hexalith.Projects.Contracts are the sole editable domain/wire vocabulary. Platform generators derive OpenAPI, clients, JSON schemas, MCP/CLI schemas, and runtime descriptors and verify them against a live host; canonical and legacy shapes use explicit adapters. Hexalith.Projects.UI.Contracts is non-packable, depends inward on Contracts, and owns only FrontComposer descriptors. This prevents editable C#/YAML/runtime authorities from drifting.
- (question) Choose command cutover safety: recommended shadow-build and compare new SDK read models from the existing event streams, switch reads by independently reversible route, then fence and drain old command ingress before atomically routing all commands to exactly one SDK writer; old HTTP/client shapes remain adapters to that writer. Command rollback is permitted only while the old writer can replay every event emitted by the new writer; otherwise mutations freeze and recovery rolls forward. Alternatives are unsafe dual command writes or a flag-day read/write replacement.
- (decision) [ADOPTED] Migration shadow-builds SDK read models from existing event streams, compares deterministic output/keys/watermarks/cursors/tenant isolation, and switches reads by independently reversible route. Command cutover fences and drains old ingress before atomically routing all commands to one SDK writer; legacy HTTP/client shapes remain adapters, never writers. Command rollback is allowed only while the old writer can replay every new event, otherwise mutations freeze and recovery rolls forward.
- (decision) [ADOPTED] The platform generates ULIDs for Project, task, message, correlation, causation, event, and receipt identities; ProjectId is reserved at Durable Task admission, stable across equivalent retries, and hidden until activation. Tenant and actor are derived from authenticated server context, and aggregate identity is server-derived as Tenant/projects/ProjectId. Foreign resource identifiers remain owner-defined opaque contract values and are never GUID-parsed.
- (question) Adopt one observable-truth mapping: authorized read computations return 200 with Complete/Partial/Unavailable in the body; Denied and nonexistent are boundary-indistinguishable 404; new or equivalent mutation admission returns 202 plus task Location and never completion; task polling returns 200 for every status; invalid input 400, stale/expired/consumed confirmation or changed idempotency request 409 with RenewPreview as applicable, overload 429, and inability to admit durably 503 with no task. SignalR carries only a versioned re-query nudge. Task Rejected is reserved for post-admission domain/owner rejection, Failed for known nonretryable technical failure, unknown partial outcomes become NeedsAttention, and cancellation reaches Cancelled only before the irreversible commit.
- (decision) [ADOPTED] Observable transport mapping is canonical across surfaces: authorized read computations return 200 with Complete/Partial/Unavailable; Denied and nonexistent collapse to safe 404; mutation admission returns 202 with task Location and never completion; task polls return 200 for every status; invalid input is 400; stale/expired/consumed confirmation and changed-request idempotency conflict are 409 with RenewPreview where applicable; admission overload is 429; inability to persist a task is 503 and creates no task. SignalR is only a versioned re-query nudge. Post-admission domain/owner denial is Rejected, known nonretryable technical failure is Failed, unknown partial outcome is NeedsAttention, and Cancelled is reachable only before irreversible commit.
- (question) Choose cross-service identity model: recommended an immutable dual-principal authorization context carrying server-derived Tenant, original actor principal, authenticated caller/workload service, delegation identifier/scopes/audience, and correlation/task IDs. Platform admission authenticates both and fails startup without issuer/audience/signing configuration; Projects evaluates actor/action policy, every resource owner reauthorizes its resource, queries filter again, and allow-all stubs cannot load outside explicit test composition. Alternatives service-only identity or blind raw-user-token forwarding lose actor authority or create confused-deputy/token exposure risk.
- (decision) [ADOPTED] Cross-service authorization uses an immutable dual-principal context with server-derived Tenant, original actor, authenticated caller/workload service, delegation identifier/scopes/audience, and correlation/task IDs. Platform admission validates credentials and fails startup on incomplete authority/audience/signing configuration; Projects evaluates actor/action/Project policy, every owning service reauthorizes its resource, and query handlers filter again. Allow-all implementations are test-only and cannot load in production-capable hosts.
- (question) Choose FR-24 execution: recommended a synchronous read-only export query over one authorized diagnostic snapshot, protected by a platform per-Tenant two-lease concurrency gate; deterministic ordering and truncation build a complete response no larger than 1 MiB with at most 500 references and 100 newest-first audit rows, then discard the buffer. Every attempt/outcome is durably metadata-audited, but export bytes are never stored and no cursor or task is created. Async generation would require payload retention or produce an unrecoverable task result.
- (decision) [ADOPTED] Safe Diagnostic Export is a synchronous read-only query over one authorized diagnostic snapshot, guarded by a platform-owned two-lease per-Tenant concurrency limit. Deterministic ordering/truncation produces one complete response at most 1 MiB with at most 500 references and 100 newest-first audit rows; unavailable components are marked safely. Every attempt/outcome is durably metadata-audited, while bytes, cursors, and tasks are never retained or created.
- (question) Choose folder-first event evolution: recommended add a serialization-tolerant optional Folder binding field to the existing ProjectCreated event contract, require it for every new write, and retain null only when replaying historical events; stop emitting ProjectFolderCreationPending but preserve its deserializer/apply path forever. Historical null-Folder streams are excluded from active read models and routed to compensating reconciliation, while ProjectFolderSet remains for legacy repair and confirmed replacement. Alternatives new V2 events or rewriting old history violate compatibility rules; persisting ProjectCreated then ProjectFolderSet recreates an observable folderless interval.
- (decision) [ADOPTED] ProjectCreated evolves additively with a serialization-tolerant optional Folder binding field that is mandatory for every new write and nullable only during historical replay. New writes stop emitting ProjectFolderCreationPending, whose type and apply path remain supported for all historical streams. Historical null-Folder state is excluded from Active reads and routed to compensating reconciliation; ProjectFolderSet remains for legacy repair and confirmed replacement.
- (decision) Restore validates or establishes an authorized Folder while the Project remains Archived; a replacement emits ProjectFolderSet before ProjectRestored in one EventStore command commit, so no Active state exists without a Folder. Restore completion still waits for the Active read model.
- (question) Choose the target package graph: recommended keep packable Hexalith.Projects.Contracts and Hexalith.Projects domain packages; introduce non-packable Hexalith.Projects.UI.Contracts for descriptors; retain Hexalith.Projects.Server only as the minimal two-line non-packable DomainService host and Hexalith.Projects.Client only as a generated contract adapter if consumers require it. Retire Projects.Infrastructure, Workers, ServiceDefaults, Aspire, AppHost, UI, MCP, and CLI runtime projects after their responsibilities move to EventStore, FrontComposer, and the platform AppHost. Tests target contracts/domain/handlers/generated conformance and platform integration. This preserves deployment naming while removing duplicate runtime ownership.
- (decision) [ADOPTED] Reduce the Projects-owned graph to Contracts, domain, non-packable UI.Contracts, the minimal two-line Server host, and an optional generated Client adapter; retire Projects-owned Infrastructure, Workers, ServiceDefaults, Aspire, AppHost, UI, MCP, and CLI runtime projects only after equivalent platform-owned local run and test paths are green. Technical responsibilities move to their technical modules, while this repository remains independently buildable, runnable, debuggable, and testable.
- (constraint) Local developer parity is a migration gate: this repository must run the Projects DomainService, EventStore/Dapr dependencies, real authentication, FrontComposer surfaces, and sibling integrations and must execute pure, host-contract, persisted-boundary, distributed-topology, and authenticated browser lanes without retaining domain-owned technical implementations.
- (version) Local platform reality: Hexalith.EventStore.Aspire already exposes AddEventStoreDomainModule; Hexalith.EventStore.Testing.Integration exposes DaprDomainServiceTestFixtureBase and AspireTopologyFixtureBase; current platform AppHosts are compile-time compositions and do not yet load Hexalith.Projects from a repository manifest. Aspire 13.4 workflow uses aspire start and aspire wait/describe for local operation.
- (question) Adopt a platform-owned generic development composition runner, invoked from this repository through a checked-in non-secret module manifest that names the Projects two-line host, domain/app id, UI.Contracts descriptors, sibling dependencies, and fixture profile. The runner owns EventStore, Dapr components, identity, FrontComposer, ports, health, telemetry, startup/teardown, and Aspire orchestration; Projects tests consume EventStore/FrontComposer test harness packages. Existing Projects AppHost/Aspire code is removed only after this runner and all local lanes prove parity.
- (decision) [ADOPTED] A platform-owned generic development composition runner loads a checked-in non-secret Projects module manifest naming the two-line host, domain/app ID, UI.Contracts descriptors, sibling dependencies, and fixture profile. The runner owns EventStore, Dapr, identity, FrontComposer, ports, health, telemetry, and Aspire lifecycle. Projects retains pure domain, host-contract, persisted-boundary, distributed-topology, and authenticated browser/CLI/MCP lanes; existing Projects AppHost/Aspire/runtime code is removed only after equivalent local run/debug/test parity passes.
- (question) Choose evidence transport: recommended two channels. Durable metadata-only audit facts derive from Project events plus platform task/confirmation/export security events and project into the 365-day audit timeline. Separate versioned metadata-only outcome signals from Projects and Chatbot flow through a platform telemetry/analytics bus into a 30-day aggregate store for SM-7/SM-8/SM-C4; they carry only state/reason/action codes, timestamps, ephemeral correlation, and correction category, are not domain events, and cannot authorize behavior. Alternative one shared event stream couples product analytics retention and failures to domain/audit truth.
- (decision) [ADOPTED] Durable audit and product analytics are separate. Project events plus platform task/confirmation/reconciliation/export security events project metadata-only audit truth under at-least-365-day retention; versioned metadata-only Projects/Chatbot outcome signals flow through a platform telemetry/analytics bus into a rolling 30-day aggregate store for SM-7/SM-8/SM-C4. Analytics carries only state/reason/action codes, timestamps, ephemeral correlation, and correction category, is not domain history, and cannot authorize behavior.
- (decision) [ADOPTED] Platform gateway/task admission centrally enforces the PRD Tenant rates, bursts, nonterminal-task cap, export leases, dependency timeouts, retries, retry guidance, and accepted-task durability; Projects enforces contract cardinality, page, reference, and export-content bounds. Domain handlers never implement infrastructure retries or per-surface quotas.
- (decision) [ADOPTED] The platform AppHost owns every environment topology, Dapr component/provider binding, identity/KMS/secret integration, mTLS/network policy, health/telemetry, container publication, deployment, and regional recovery policy. Projects supplies no provider-specific infrastructure and only declares required capabilities and configuration schema; local, CI, and production use the same logical resource contract with environment-specific platform bindings.
- (decision) [ADOPTED] MCP is a platform-composed operator/admin adapter over the same versioned contracts and dual-principal authorization. It cannot confirm end-user resolution/proposal choices, bypass Preview/Confirmation/Task admission, or expand authority; consequential MCP mutation remains disabled until readiness and release gates pass, and tool inventory/interaction presentation is deferred to UX while these containment rules bind it.
- (decision) [ADOPTED] Release evidence is machine-checkable and maps every FR, NFR, P1/P2 finding, AD, repository/version gate, story owner, fixture, command, artifact, and terminal disposition. Failed critical cases or unexplained critical skips block; unavailable environments remain not verified. The architecture binds evidence ownership but detailed test matrices remain test-design seed.
- (direction) Coaching elicitation complete; distill the architecture spine from the memlog and current brownfield evidence, keeping the spine as the only deliverable.
- (evidence by architect) Version check 2026-07-16: repository pins .NET SDK 10.0.300/C# 14; official .NET 10 remains active LTS, while 10.0.301 is the current SDK servicing release. Preserve 10.0.300 for reproducibility and upgrade only through the repository dependency process.
- (evidence by architect) Version check 2026-07-16: repository pins Aspire 13.4.6; official Aspire 13.4 is the current compatible product line. Aspire remains platform-owned and must not leak into the Projects domain module.
- (gap by architect) Dapr version gate: shared package baseline mixes Dapr CLI/runtime 1.17.9 with .NET SDK/Workflow 1.18.4, while official Dapr 1.18 is current. Platform migration must align and prove the runtime/SDK seam before adopting generic durable workflow behavior; Projects never binds directly to Dapr Workflow.
- (risk by architect) Fluent UI gate: shared baseline pins Microsoft Fluent UI Blazor 5.0.0-rc.4-26180.1, whereas the official stable release line is 4.14.3. Treat the v5 RC as platform presentation risk and require compatibility evidence; it is not a Projects domain dependency.
- (decision by architect) Named technology versions in the spine are exact repository pins or explicit migration gates; generated schemas and clients are platform-owned outputs and must not silently track latest OpenAPI or tool versions.
- (correction by reviewer) Final tech review: .NET global.json sets feature-band baseline 10.0.300 with latestPatch and the reviewed environment resolves 10.0.301; the spine must distinguish policy baseline from effective SDK.
- (correction by reviewer) Final tech review: root package mode pins FrontComposer 4.0.0 while the checked-out source is 4.0.1; G-3 must record the patch disposition and parity before use.
- (correction by reviewer) Final tech review: Dapr runtime 1.18.0 plus .NET packages 1.18.4 is current local evidence, not an automatically supported release tuple; G-6 requires authoritative support-matrix alignment or an approved support exception plus live evidence.
- (decision by reviewer) Stack is split into target/compatibility bindings and migration/catalog-only inventory; EventStore API claims require published or clean 3.67.3 evidence, and prerelease/stale pins cannot silently become target dependencies.
- (event by architect) Architecture finalized 2026-07-16 after PRD, course-correction, readiness, brownfield, rubric, technology-current, and adversarial reconciliation all passed; deterministic spine lint is clean and the May architecture is visibly superseded.
